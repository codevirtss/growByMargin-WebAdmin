import 'package:flutter/material.dart';

class ViewUserMobile extends StatelessWidget {
  const ViewUserMobile({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}