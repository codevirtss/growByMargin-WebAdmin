import 'package:flutter/material.dart';

class ViewUserTablet extends StatelessWidget {
  const ViewUserTablet({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}